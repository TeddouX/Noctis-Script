obj TestObj {
    pub Int i = 0;

    TestObj(Int newI, Int newB, Int newC) {}
}

obj Vec2 {
    pub Int x = 0;
    pub Int y = 0;

    pub TestObj test = new TestObj(10, 10, 10);

    priv Vec2() {
        //x = 10;
        //y = 100;
    }

    pub Int Add(Int a, Int b) {
        Int res = a + b;
        return res;
    }

    pub Int AddMembers() {
        return x + y;
    }
}

fun Add(Int a, Int b) {
    Int c = a + b;
}

fun Main() {
    Vec2 a = new Vec2();
    Vec2 b = new Vec2();
    
    TestObj c = new TestObj(10, 10, 10);
    TestObj d = new TestObj(10, 10, 10);

    a.test.i = 2;
    a.test.i *= 123;

    printHello(a.x);
    printHello(a.test.i);
}
