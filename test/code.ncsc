obj TestObj {
    pub Int i = 0;

    pub TestObj() {}
}

obj Vec2 {
    pub Int x = 0;
    pub Int y = 0;

    pub TestObj test = new TestObj();

    pub Vec2(Int x, Int y) {
        this.x = x;
        this.y = y;
    }

    pub Int AddMembers() { return x + y; }
    pub TestObj GetTest() { return test; }
}

fun Add(Int a, Int b) {
    Int c = a + b;
}

fun Main() {
    // Vec2 v1 = new Vec2(10, 10);
    // Vec2 v2 = v1;
    // v2.x = 11;

    // printHello(v1.x);

    Vec3 v3 = new Vec3(10, 10, 10);
    printHello(v3.x);

    v3.x = 20;

    printHello(v3.x);

    printHello(v3.AddMembers()); 
}
