obj TestObj {
    pub Int i = 0;

    pub TestObj(Int newI, Int newB, Int newC) {}
}

obj Vec2 {
    pub Int x = 0;
    pub Int y = 0;

    pub TestObj test = new TestObj(10, 10, 10);

    pub Vec2(Int x, Int y) {
        this.x = x;
        this.y = y;
    }

    pub Int Add(Int a, Int b) {
        Int res = a + b;
        return res;
    }

    pub Int AddMembers() {
        return x + y;
    }

    pub Int AddMembers1(Int i) {
        return x + y + i;
    }

    pub TestObj GetTest() { return test; }
}

fun Add(Int a, Int b) {
    Int c = a + b;
}

fun Main() {
    Vec2 v1 = new Vec2(10, 10);
    Vec2 v2 = v1;
    v2.x = 11;

    printHello(v1.x);
}
