obj Vec2 {
    pub Int x = 0;
    pub Int y = 0;

    pub Vec2(Int x, Int y) {
        this.x = x;
        this.y = y;
    }

    pub Int AddMembers() { return x + y; }
}

// 1 less than necessary to trigger the garbage collector
fun FunctionThatCreates9Objects() {
    // Vec3 v1 = new Vec3(1,1,1);
    // Vec3 v2 = new Vec3(1,1,1);
    // Vec3 v3 = new Vec3(1,1,1);
    // Vec3 v4 = new Vec3(1,1,1);
    // Vec3 v5 = new Vec3(1,1,1);
    // Vec3 v6 = new Vec3(1,1,1);
    // Vec3 v7 = new Vec3(1,1,1);
    // Vec3 v8 = new Vec3(1,1,1);
    // Vec3 v9 = new Vec3(1,1,1);
}

fun Main() {
    FunctionThatCreates9Objects();
    // Trigger the garbage collector
    // Vec3 v2 = new Vec3(1,1,1);

    Vec3 v1 = new Vec3(2,1,3);
    v1.x++;

    Vec2 v3 = new Vec2(1, 1);
    v3.x++;

    printHello(v1.AddMembers());
    printHello(v3.AddMembers());
}
