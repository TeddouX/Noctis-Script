// Int global = 10;

fun main() {
    // Int a = 1 + 2 * 3;
    Int b = 3;

    if (b < 1) {
        printHello(0);
    } else if (b == 2) {
        printHello(1);
    } else {
        printHello(2);
    }

    printHello(3);

    // Int c = global;

    return;
}
